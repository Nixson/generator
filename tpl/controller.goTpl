package controller

import (
	"github.com/Nixson/http/server"
	ctr "${path}"
)

func InitController${name}() {
	ctx := server.ContextInterface(&ctr.${name}{})
	server.InitController("${name}", &ctx)
}
